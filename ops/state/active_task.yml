id: cliq-195
title: package cliq workflows for agent operation (internal loop + external-contact escalation path)
module: cliq
stage: in_progress
status: in_progress
owner: coder
updated_at: '2026-04-24T06:09:01Z'
success_criteria:
- add an initial cliq-195 operator-workflow packaging contract to watch-context output without breaking existing watch-loop consumers
- preserve the operator-workflow contract through watch-act action payload/result surfaces used by automation loops
- keep focused watch-context/watch-act verification green while deferred external blockers stay capability-gated
next_if_done: if focused and medium-scope cliq-195 verification remain green, continue the next operator-workflow packaging increment without regressing deferred capability-gated blockers.
next_step: "ran the queued live unread-filter validation and medium-scope cliq-195 integration rerun at 2026-04-24T06:09:01Z: medium-scope suites stayed green (`./.venv/bin/python -m pytest -q tests/test_registry.py tests/test_membrane_bridge.py` -> `387 passed in 6.73s`; `./.venv/bin/python -m pytest -q tests/test_cli.py -k 'cliq_watch_context_from_channel or cliq_watch_act or watch_act_read_ack_latest or cliq_context_with_anchor or cliq_messages_from_channel or cliq_message_get_from_channel or cliq_bridge_run or crm_bridge_run'` -> `291 passed, 424 deselected in 7.07s`; `./.venv/bin/python -m pytest -q tests/test_cliq.py -k 'build_watch_context_seed or build_watch_reply_action or execute_watch_reply_action or build_watch_read_ack_action or execute_watch_read_ack_action or list_messages_from_channel_id or get_message_from_chat_id or get_message_from_channel_id or reply_message_success or edit_message_success or delete_message_success or react_message'` -> `24 passed, 165 deselected in 0.11s`; CLI help smoke -> `HELP_SMOKE_OK`; `./.venv/bin/ruff format --check zoho_cli/cli.py tests/test_cli.py tests/test_membrane_bridge.py` -> `3 files already formatted`), but live `cliq chats --unread-only --exclude-reacted-by-self` is now blocked on local config credentials (`missing_credentials: client_id and client_secret must be set. Run: zoho config init`), so the next best step is to restore ai-dev credentials in `/tmp/zoho-test-config.json` and rerun the live unread-filter probe before the next thin cliq-195 packaging increment."
