id: cliq-165
title: verify cliq export-chats live with organization export scopes and archive sample output
module: cliq
stage: blocked
status: blocked
owner: automation
updated_at: '2026-04-12T19:24:46Z'
success_criteria:
- verify `cliq export-chats` list succeeds on happydistrouklimited with export scopes granted
- verify `cliq export-chats --chat-id ...` succeeds and archive one sample output JSON
- keep blocker-skipping loop active when API-side app-account activation is still pending
next_if_done: start cliq-191 with one smallest command/client slice while keeping cliq export verification evidence fresh
next_step: cliq-190 live verification is now archived (`tests/auto_pilot/reports/cliq190_probe_summary_20260412_192446.json`); coordinate Cliq admin-side app-account activation and rerun `cliq export-chats` list + `--chat-id` on /tmp/zoho-test-config.json