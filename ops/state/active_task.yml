id: cliq-193
title: implement app-governance plane (apps/app-get/app-permissions/app-permission-get/app-installs/app-install-get/app-commands/app-command-get complete, continue next slice)
module: cliq
stage: in_progress
status: in_progress
owner: automation
updated_at: '2026-04-14T07:48:19Z'
success_criteria:
- ship the next cliq-193 app-governance command/client slice with focused tests
- run one focused live verification for that new slice and archive evidence
- keep cliq-165 export blocker evidence current while app-account activation remains external
next_if_done: take the next queued cliq-193 app-governance slice with focused coverage
next_step: "unit hardening complete: `cliq app-installs` now unwraps deeper wrapper stacks for record-list maintenance payloads by extending install-row unwrapping depth (8 -> 16) and unwrapping first dict entries from list-valued wrapper keys (`record`/`items`-style), with focused tests green (`tests/test_cli.py::test_cliq_app_installs_accepts_subject_id_alias`, `tests/test_cli.py::test_cliq_app_installs_accepts_top_level_payload_wrapper_shape`, `tests/test_cli.py::test_cliq_app_installs_accepts_deep_data_records_record_item_wrapper_shape`). next is one focused live rerun (`cliq status --check-auth` + `cliq app-installs APP_PROBE_FAKE_<stamp> --limit 10`) and evidence archive while cliq-165 remains externally blocked by `inactive_appaccount_user`."
