id: cliq-156
title: implement reliable local media send (image/voice/file) for user/channel targets
module: cliq
stage: in_progress
status: in_progress
owner: automation
updated_at: '2026-04-12T02:12:16Z'
success_criteria:
  - capture a cooldown-safe endpoint/field matrix for local image/voice/file sends across user and channel targets
  - preserve deterministic `not_supported` classification when all live candidates miss with endpoint-limitation patterns
  - obtain at least one attachment-positive live send+retrieval sample, or document per-endpoint limitation evidence sufficient for cliq-157 handoff
  - keep local multipart diagnostics compact and machine-readable (`attempts` path/field/status/code tuples)
  - validate with focused unit tests and one live matrix rerun on current token/network
next_if_done: cliq-157
next_step: "implement a cooldown-safe local-media matrix runner (image/voice/file, user+channel) that spaces live calls to avoid refresh throttling, then archive one evidence bundle for cliq-156"
