id: cliq-155
title: implement dm/group/channel conversation visibility and typed context matrix
module: cliq
stage: in_progress
status: in_progress
owner: automation
updated_at: '2026-04-12T00:24:23Z'
success_criteria:
  - provide chat discovery command surface (`cliq chats`) with explicit scope/not-supported diagnostics
  - keep per-conversation context outputs machine-readable with inferred message content types (`text`, `image`, `file`, `voice`, `sticker`, `reaction`)
  - maintain channel/chat context extraction for agent loops (`messages`, `message`, `context`) with typed summaries
  - validate via focused tests and at least one live smoke path on current token/network
next_if_done: cliq-160
next_step: "token refresh on `/tmp/zoho-test-config.json` is healthy again, but current token still has Mail-only scopes; run `zoho login --with-cliq` (optional extra scopes can now be passed as either repeatable `--scope` flags or a comma-delimited value), then rerun live cliq-155 upload/download matrix probes (voice/image/file + file/voice retrieval) via `tests/auto_pilot/run_cliq_live_probe.sh` (now root-stable from either repo root or workspace root) and capture endpoint + field + status/code evidence from `attempts:` tuples plus one attachment-positive retrieval sample"
