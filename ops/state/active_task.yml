id: cliq-193
title: implement app-governance plane (apps/app-get/app-permissions/app-permission-get/app-installs/app-install-get/app-commands/app-command-get complete, continue next slice)
module: cliq
stage: in_progress
status: in_progress
owner: automation
updated_at: '2026-04-15T09:32:58Z'
success_criteria:
- ship the next cliq-193 app-governance command/client slice with focused tests
- run one focused live verification for that new slice and archive evidence
- keep cliq-165 export blocker evidence current while app-account activation remains external
next_if_done: take the next queued cliq-193 app-governance slice with focused coverage
next_step: "completed cliq-193 app-command list hardening so metadata-first wrapper traversal now prefers entries that carry command hints even when wrappers are list-shaped; wrapped action rows (for example `{payload: {action_id: ...}}`) are now selected ahead of wrapped metadata rows, preventing false command loss in `payload.data` lists. focused tests are green (`tests/test_cli.py::test_cliq_app_commands_prefers_wrapped_action_row_over_wrapped_metadata_in_payload_data_list`, `tests/test_cli.py::test_cliq_app_commands_prefers_action_hint_row_with_meta_over_metadata_only_data_list`, `tests/test_cli.py::test_cliq_app_commands_skips_metadata_only_data_list_and_uses_commands_list`). next: run the matching focused live rerun (`cliq status --check-auth` + `cliq app-commands APP_PROBE_FAKE_<stamp>`) while cliq-165 remains externally blocked on `inactive_appaccount_user`."
