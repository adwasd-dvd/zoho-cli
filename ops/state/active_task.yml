id: cliq-195
title: package cliq workflows for agent operation (internal loop + external-contact escalation path)
module: cliq
stage: in_progress
status: in_progress
owner: coder
updated_at: '2026-04-19T00:13:00Z'
success_criteria:
- add an initial cliq-195 operator-workflow packaging contract to watch-context output without breaking existing watch-loop consumers
- preserve the operator-workflow contract through watch-act action payload/result surfaces used by automation loops
- keep focused watch-context/watch-act verification green while deferred external blockers stay capability-gated
next_if_done: if focused watch-context/watch-act/bridge-run checks remain green with top-level escalation-envelope aliasing, continue with one thin follow-up that has external-contact adapter paths consume `escalationEnvelope` first with nested handoff-default fallback retained for compatibility.
next_step: add one thin cliq-195 follow-up that wires adapter-facing external-contact paths to prefer top-level `escalationEnvelope` on watch/bridge payloads while preserving fallback to `operatorWorkflow.externalEscalation.handoff.envelopeDefaults`.
