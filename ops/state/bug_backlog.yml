blockers:
  - "crm-002 live smoke remains blocked: current test account is not in a CRM org, so CRM scopes are not granted and live calls return `OAUTH_SCOPE_MISMATCH` (verified 2026-04-08T15:49:49Z)"
high: []
normal:
  - "cliq-140 live search verification on happydistrouklimited returns unsupported endpoint patterns (`extra_param_found` on `/messages/search` with query key attempts and `request_url_invalid` on fallback paths); command now reports `not_supported` with capabilities hint"
  - "cliq-155 chat discovery live positive-path is still blocked on current token/network: `/chats` returns `oauthtoken_scope_invalid` until chat-read scope is granted via re-auth; local multipart upload path now tries raw+resolved user targets plus direct `/channels/{channel_id}/message` fallback, and standard channel-id sends now probe the same direct channel endpoint; current live multipart probes still fail with endpoint-level `request_url_invalid` / `operation_failed`, so matrix evidence remains pending"
low: []
