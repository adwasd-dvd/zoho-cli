mail:
  priority: 1
  status: completed
  phase: stabilization_complete
  target_release: v0.2.0
  current_milestone: mail-release-gate
  blockers: []

cliq:
  priority: 2
  status: in_progress
  phase: expansion_conversation_visibility_and_media_primitives
  target_release: v0.4.0
  current_milestone: cliq-expansion-phase
  blockers:
    - "live search endpoint variants are currently unsupported on happydistrouklimited (`extra_param_found` / `request_url_invalid`)"
    - "`/chats` listing is scope-blocked on current token/network (`oauthtoken_scope_invalid`), so DM/group discovery is not yet closed"
    - "native local-file upload/download endpoint variants still need a stable cross-network matrix; multipart baseline now returns upload endpoint/field metadata and `cliq file` reports retrieval `sourcePath`, user-target sends now fallback across raw email + resolved user id, and standard channel-id sends now also probe `/channels/{channel_id}/message`, but live verification evidence is still pending because current multipart probes return `request_url_invalid` / `operation_failed`"

crm:
  priority: 3
  status: blocked
  phase: phase_1_read_only_commands_implemented
  target_release: v0.5.0
  current_milestone: crm-phase1-read-only
  blockers:
    - test account is not in a CRM org; live CRM scopes cannot be granted
