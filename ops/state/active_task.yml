id: cliq-193
title: implement app-governance plane (apps/app-get/app-permissions/app-permission-get/app-installs/app-install-get/app-commands/app-command-get complete, continue next slice)
module: cliq
stage: in_progress
status: in_progress
owner: automation
updated_at: '2026-04-14T16:41:01Z'
success_criteria:
- ship the next cliq-193 app-governance command/client slice with focused tests
- run one focused live verification for that new slice and archive evidence
- keep cliq-165 export blocker evidence current while app-account activation remains external
next_if_done: take the next queued cliq-193 app-governance slice with focused coverage
next_step: "cliq-193 command-id alias hardening is complete: `zoho cliq app-commands` + `zoho cliq app-command-get` now also accept Pascal/camel variants (`ActionId` / `ActionID` / `actionID`) in addition to `actionId` / `action_id` (focused tests: `tests/test_cli.py::test_cliq_app_commands_accepts_action_id_alias_shape`, `tests/test_cli.py::test_cliq_app_commands_accepts_action_id_pascal_case_alias_shape`, `tests/test_cli.py::test_cliq_app_command_get_accepts_action_id_alias_shape`, `tests/test_cli.py::test_cliq_app_command_get_accepts_action_id_pascal_case_alias_shape`). next is one focused live rerun (`cliq status --check-auth` + `cliq app-commands APP_PROBE_FAKE_<stamp>`) while cliq-165 remains externally blocked by `inactive_appaccount_user`."
