# Current Focus

## Immediate
1. **`cliq-181` is now closed with live evidence**: the `cliq pinned` probe on happydistrouklimited returns `not_supported`, so chat-control coverage is complete in code and endpoint behavior is classified.
2. **`cliq-190` fifth slice is now landed**: `zoho cliq user-status` + `ZohoCliqClient.list_user_statuses` are in place with targeted tests.
3. **Keep blocker-skipping loop active**: do not stall on `cliq-165` export-scope interactive re-auth or known endpoint limitations.

## Next
1. `cliq-190`: continue with the next org-admin phase-1 command/client slice (userfields)
2. `cliq-165`: rerun export verification after interactive re-auth with maintenance export scopes
3. `crm-002`: remain blocked until CRM-enabled org access is available
