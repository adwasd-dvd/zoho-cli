id: cliq-193
title: implement app-governance plane (apps/app-get/app-permissions/app-permission-get/app-installs/app-install-get/app-commands/app-command-get complete, continue next slice)
module: cliq
stage: in_progress
status: in_progress
owner: automation
updated_at: '2026-04-15T21:24:53Z'
success_criteria:
- ship the next cliq-193 app-governance command/client slice with focused tests
- run one focused live verification for that new slice and archive evidence
- keep cliq-165 export blocker evidence current while app-account activation remains external
next_if_done: take the next queued cliq-193 app-governance slice with focused coverage
next_step: "completed a focused cliq-193 unit hardening increment: app-command list/detail normalization now accepts camel+snake display-name aliases (`commandDisplay_name` / `actionDisplay_name`) in command-row hint detection and output name mapping; focused tests + format check passed (`tests/test_cli.py::test_cliq_app_commands_accepts_camel_snake_command_display_name_alias_shape`, `tests/test_cli.py::test_cliq_app_command_get_accepts_camel_snake_action_display_name_alias_shape`, `./.venv/bin/ruff format --check zoho_cli/cli.py tests/test_cli.py`). next: run one matching focused live rerun (`cliq status --check-auth` + `cliq app-commands APP_PROBE_FAKE_<stamp>`) while `cliq-165` remains externally blocked on `inactive_appaccount_user`."
