module: CRM
updated_at: "2026-05-12T22:22:41Z"
status: in_progress
phase: v0_5_cleanup_plan_template
blockers:
  - "operator-provided dedicated CRM fixture payload and cleanup approval required before Zoho-backed dry-run smoke and live fixture execution"
current_milestone: crm-033-cleanup-plan-template
last_completed:
  id: crm-033
  summary: "Added `docs/releases/CRM_V0_5_FIXTURE_CLEANUP_PLAN_TEMPLATE.md` and linked the cleanup-plan operator request from the RC autonomy/operator-action prompts so operators have a safe copy/edit starter without granting agents live write permission."
next_small_step: run `ops/scripts/zoho_cli_rc_autonomy_packet.sh` first; when it requests CRM inputs, use `ops/scripts/zoho_cli_rc_operator_action_prompt.sh --md` to ask for the operator-selected Cliq publish path plus copied CRM fixture payload and copied cleanup plan, then run `ops/scripts/crm_fixture_agent_next_command.sh` only if it reports `status=agent_next_command_ready` and `safety.nextCommandAllowedForAgent=true`
