id: cliq-155
title: implement dm/group/channel conversation visibility and typed context matrix
module: cliq
stage: in_progress
status: in_progress
owner: automation
updated_at: '2026-04-12T00:31:30Z'
success_criteria:
  - provide chat discovery command surface (`cliq chats`) with explicit scope/not-supported diagnostics
  - keep per-conversation context outputs machine-readable with inferred message content types (`text`, `image`, `file`, `voice`, `sticker`, `reaction`)
  - maintain channel/chat context extraction for agent loops (`messages`, `message`, `context`) with typed summaries
  - validate via focused tests and at least one live smoke path on current token/network
next_if_done: cliq-160
next_step: "Cliq auth now reports `oauthReady: true`; use cooldown-safe batches to finish cliq-155 matrix closure: (1) rerun local media probes after refresh-throttle cooldown, (2) collect at least one attachment-positive retrieval sample via `cliq file`/`cliq voice`, and (3) classify persistent DM/local-upload endpoint failures (`request_url_invalid` / `operation_failed` / `request_method_invalid`) by path+field from `attempts:` evidence"
