id: cliq-193
title: implement app-governance plane (apps/app-get/app-permissions/app-permission-get/app-installs/app-install-get/app-commands/app-command-get complete, continue next slice)
module: cliq
stage: in_progress
status: in_progress
owner: automation
updated_at: '2026-04-13T12:05:12Z'
success_criteria:
- ship the next cliq-193 app-governance command/client slice with focused tests
- run one focused live verification for that new slice and archive evidence
- keep cliq-165 export blocker evidence current while app-account activation remains external
next_if_done: take the next queued cliq-193 app-governance slice with focused coverage
next_step: "delivered a small cliq-193 output-shape hardening follow-up so `cliq app-commands` and `cliq app-command-get` now unwrap nested singular `action` wrappers (including `data.action`) and map `action` strings into command names when canonical name fields are absent; focused CLI regression tests are green (4 passed), and the next step is one focused live verification pass for `cliq app-command-get` while cliq-165 stays externally blocked by inactive_appaccount_user"
