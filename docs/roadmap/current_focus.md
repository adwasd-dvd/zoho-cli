# Current Focus

## Immediate
1. **`cliq-181` is now closed with live evidence**: the `cliq pinned` probe on happydistrouklimited returns `not_supported`, so chat-control coverage is complete in code and endpoint behavior is classified.
2. **`cliq-190` sixth slice is now landed**: `zoho cliq userfields` + `ZohoCliqClient.list_user_fields` are in place with targeted tests.
3. **Keep blocker-skipping loop active**: do not stall on `cliq-165` app-account export activation or known endpoint limitations.

## Next
1. `cliq-190`: run one live `cliq userfields` verification pass and classify endpoint behavior on happydistrouklimited
2. `cliq-165`: rerun export verification after Cliq admin-side app-account activation
3. `crm-002`: remain blocked until CRM-enabled org access is available
