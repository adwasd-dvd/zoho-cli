id: cliq-193
title: implement app-governance plane (apps/app-get/app-permissions/app-permission-get/app-installs/app-install-get/app-commands/app-command-get complete, continue next slice)
module: cliq
stage: in_progress
status: in_progress
owner: automation
updated_at: '2026-04-14T01:05:06Z'
success_criteria:
- ship the next cliq-193 app-governance command/client slice with focused tests
- run one focused live verification for that new slice and archive evidence
- keep cliq-165 export blocker evidence current while app-account activation remains external
next_if_done: take the next queued cliq-193 app-governance slice with focused coverage
next_step: "unit hardening is complete for install payload envelopes (`cliq app-installs` + `cliq app-install-get` now unwrap top-level `payload` wrappers with nested `response` / `result` / `data.records.record.item` shapes); next step is one focused live cliq-193 rerun (`cliq status --check-auth` + `cliq app-install-get APP_PROBE_FAKE_<timestamp> INSTALL_PROBE_FAKE_<timestamp>`) and archive evidence, then take the next small output-hardening increment."
