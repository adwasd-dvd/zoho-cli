# Current Focus

## Immediate
1. **Cliq read parity first**: fully stabilize DM / channel / group message reads, context extraction, and typed message outputs.
2. **Cliq media send first-class**: ship reliable local image / voice / file send flows (user + channel targets), not just link-card fallback.
3. **Zoho API matrix digestion**: classify endpoint/method/form-field behavior by network and capture stable fallback strategy in code + state evidence.
4. Close `cliq-155` with attachment-positive retrieval evidence and failure taxonomy (code defect vs endpoint limitation).

## Next
1. `cliq-156`: media upload endpoint hardening + retries/cooldown-safe probe loop
2. `cliq-160`: release-gate hardening after Cliq live matrix closure
3. CRM implementation work (unblocked only when CRM org access is available)
