id: cliq-channel-415
title: implement observability privacy retention rate-limit diagnostics and npm integrity hardening
module: openclaw_cliq_channel
stage: ready
status: ready
owner: coder
updated_at: '2026-05-05T08:25:45Z'
execution_mode: turbo_20x
throughput_policy_ref: ops/state/execution_policy.yml
success_criteria:
  - add redacted native dispatch/ingress audit events with correlation ids that never include webhook secrets, token passwords, raw signatures, or raw message bodies
  - expose rate-limit, webhook, polling, lifecycle, turn-ledger, and native dispatch diagnostics in status/capability summaries
  - document privacy retention boundaries for Cliq message metadata, dead-letter entries, and local diagnostic bundles
  - add dead-letter replay guidance that requires explicit operator action and preserves loop-prevention semantics
  - add npm artifact integrity/release checklist placeholders without blocking local linked-package development
  - update Lane 2 docs, Lane 3 skill/docs, and Lane 1 state/progress docs
  - keep package-local `npm run typecheck`, `npm run build`, OpenClaw linked install, `plugins inspect zoho-cliq --json`, `plugins doctor`, focused tests, and `make ci` green
next_if_done: run cliq-channel-411 fake and live verification gate for v0.4 native Cliq channel.
progress:
  - "2026-05-05T08:25:45Z cliq-channel-417 completed: `src/native-dispatch.ts` maps accepted Cliq webhook/polling events into OpenClaw native channel turns, records session/last-route metadata, delivers agent replies through the Cliq outbound adapter, and fake host coverage verifies trusted webhook dispatch, trusted polling dispatch, denied events, duplicate suppression, and dispatch-failure dead-lettering. The next active slice is `cliq-channel-415` observability/privacy hardening."
  - "2026-05-05T08:01:29Z cliq-channel-410 completed: root skill, native channel skill, Lane 3 guide, setup runbook, development guide, README, and lane3 docs tests now map setup states, diagnostic blockers, explicit route formats, and production blockers to AI-safe next actions. The next active slice is corrected to `cliq-channel-417` native agent turn dispatch before observability, so the channel line stays focused on Discord-like native behavior."
next_step: Start `cliq-channel-415` by adding redacted diagnostics, privacy/retention guidance, dead-letter replay rules, and npm integrity release hardening.
