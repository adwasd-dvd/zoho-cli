id: cliq-195
title: package cliq workflows for agent operation (internal loop + external-contact escalation path)
module: cliq
stage: in_progress
status: in_progress
owner: coder
updated_at: '2026-04-27T07:27:21Z'
success_criteria:
- add an initial cliq-195 operator-workflow packaging contract to watch-context output without breaking existing watch-loop consumers
- preserve the operator-workflow contract through watch-act action payload/result surfaces used by automation loops
- keep focused watch-context/watch-act verification green while deferred external blockers stay capability-gated
next_if_done: if focused and medium-scope cliq-195 verification remain green, continue the next operator-workflow packaging increment without regressing deferred capability-gated blockers.
next_step: "focused cliq-195 final alias-audit increment at 2026-04-27T07:27:21Z generalized mixed consume-policy root alias extraction in both watch-loop action inference (`zoho_cli/cli.py::_extract_watch_consume_with_source`) and read-ack requirement parsing (`zoho_cli/cliq.py::_watch_consume_requires_read_ack`), so non-canonical roots like `consume-Poli_cy` now resolve in watch-act/bridge-run plus watch-reply auto-read flows without one-off alias churn; focused coverage stayed green (`./.venv/bin/python -m pytest -q tests/test_cliq.py::test_execute_watch_reply_action_honors_mixed_consume_policy_alias tests/test_cli.py::test_cliq_watch_act_accepts_mixed_consume_policy_action_alias tests/test_membrane_bridge.py::test_cliq_bridge_run_watch_file_accepts_mixed_consume_policy_action_alias` -> `3 passed in 2.25s`; `./.venv/bin/ruff format --check zoho_cli/cliq.py zoho_cli/cli.py tests/test_cliq.py tests/test_cli.py tests/test_membrane_bridge.py` -> `5 files already formatted`); next best step is one final medium-scope cliq-195 refresh rerun and explicit done-line cut if it remains green."
