module: CRM
updated_at: "2026-05-12T14:13:11Z"
status: in_progress
phase: v0_5_agent_automation_boundary
blockers:
  - "operator-provided dedicated CRM fixture payload and cleanup approval required before Zoho-backed dry-run smoke and live fixture execution"
current_milestone: crm-028-agent-automation-boundary
last_completed:
  id: crm-028
  summary: "Added `operatorReview.agentAutomation` to CRM fixture operator packets and readiness bundles so agents can read `nextAgentExecutableCommandId`, `agentMayExecuteNextCommand`, `stopCommandIds`, and `stopReason` without inferring from command-preview arrays."
next_small_step: run `ops/scripts/crm_fixture_operator_packet.sh` against an operator-edited dedicated fixture payload and cleanup plan; execute only when `operatorReview.agentAutomation.agentMayExecuteNextCommand=true`, and stop on any `stopCommandIds`
