blockers:
  - "crm-002 live smoke remains blocked: current test account is not in a CRM org, so CRM scopes are not granted and live calls return `OAUTH_SCOPE_MISMATCH` (verified 2026-04-08T15:49:49Z)"
high: []
normal:
  - "cliq-193 focused live verification (`cliq status --check-auth` + `cliq app-commands APP_PROBE_FAKE_20260413_050402`) on happydistrouklimited returns `not_supported` for app-command endpoints while auth/export readiness remains healthy (`oauthReady: true`, `exportOauthReady: true`) (evidence: `tests/auto_pilot/reports/cliq193_app_commands_probe_summary_20260413_050402.json`, `tests/auto_pilot/reports/cliq193_status_20260413_050402.json`, `tests/auto_pilot/reports/cliq193_app_commands_20260413_050402.json`)"
  - "cliq-193 focused live verification (`cliq status --check-auth` + `cliq app-installs APP_PROBE_FAKE_20260413_042249`) on happydistrouklimited returns `not_supported` for app-install endpoints while auth/export readiness remains healthy (`oauthReady: true`, `exportOauthReady: true`) (evidence: `tests/auto_pilot/reports/cliq193_app_installs_probe_summary_20260413_042249.json`, `tests/auto_pilot/reports/cliq193_status_20260413_042249.json`, `tests/auto_pilot/reports/cliq193_app_installs_20260413_042249.json`)"
  - "cliq-193 focused live verification (`cliq status --check-auth` + `cliq app-permissions APP_PROBE_FAKE_20260413_034304`) on happydistrouklimited returns `not_supported` for app-permissions endpoints while auth/export readiness remains healthy (`oauthReady: true`, `exportOauthReady: true`) (evidence: `tests/auto_pilot/reports/cliq193_app_permissions_probe_summary_20260413_034304.json`, `tests/auto_pilot/reports/cliq193_status_20260413_034304.json`, `tests/auto_pilot/reports/cliq193_app_permissions_20260413_034304.json`)"
  - "cliq-192 focused live platform-extension bundle on happydistrouklimited shows mixed blockers with auth healthy (`oauthReady: true`, `exportOauthReady: true`): `cliq databases` needs `ZohoCliq.Databases.READ`, `cliq custom-domains` needs `ZohoCliq.CustomDomains.READ`, and `cliq widgets`/`cliq map-tickers`/`cliq custom-emails` are endpoint-limited (`not_supported`) (evidence: `tests/auto_pilot/reports/cliq192_platform_extension_bundle_summary_20260413_002232.json` + per-command bundle artifacts)"
  - "cliq-191 focused live meetings probe on happydistrouklimited returns `not_supported`, so calls/meetings endpoints are currently endpoint-limited on this token/network even after `/calls` + `/admin/calls` fallback hardening (evidence: `tests/auto_pilot/reports/cliq191_probe_summary_20260412_210519.json`, `tests/auto_pilot/reports/cliq191_meetings_20260412_210519.json`)"
  - "cliq-181 live `cliq pinned` probe on happydistrouklimited returns `not_supported`, so pinned retrieval endpoints are currently endpoint-limited on this token/network while auth remains healthy (`oauthReady: true`) (evidence: `tests/auto_pilot/reports/cliq181_probe_summary_20260412_164540.json`, `tests/auto_pilot/reports/cliq181_pinned_20260412_164540.json`)"
  - "cliq-180 live `trigger-bot` probe on happydistrouklimited returns `not_supported`, so bot trigger/call endpoints are currently endpoint-limited on this token/network (evidence: `tests/auto_pilot/reports/cliq180_trigger_bot_20260412_150542.json`; baseline capability snapshot still healthy: `tests/auto_pilot/reports/cliq180_capabilities_20260412_150542.json`)"
  - "cliq-171 live scheduled-message probe bundle on happydistrouklimited classifies schedule endpoints as unsupported for the current token/network: `schedule`, `scheduled`, `scheduled-get`, and `scheduled-cancel` all return `not_supported` while auth remains healthy (`tests/auto_pilot/reports/cliq_scheduled_probe_summary_20260412_132412.json`)"
  - "cliq-170 live thread probe bundle on happydistrouklimited now classifies thread endpoints as unsupported for the current token/network: `thread-create`, `thread-reply`, `thread-followers`, `thread-state`, and `thread-state --state ...` return `not_supported` while `threads` returns `count: 0` (evidence: `tests/auto_pilot/reports/cliq_thread_probe_summary_20260412_044149.json`)"
  - "cliq-172 targeted live probe confirms search + retrieval limits persist on happydistrouklimited: `cliq search SCAP` returns `not_supported`, and media-typed message retrieval via `cliq file`/`cliq voice` still reports `media_visible_without_attachment_payload` (`count: 0`) (evidence: `tests/auto_pilot/reports/cliq172_probe_summary_20260412_134514.json`)"
  - "`cliq export-chats` live verification is now blocked by API-side `inactive_appaccount_user` (maintenance export scopes are already granted on `/tmp/zoho-test-config.json`); list + explicit `--chat-id` probes both fail until Cliq admin-side app-account activation is completed (evidence: `tests/auto_pilot/reports/cliq_export_chats_list_inactive_recheck_20260412_105646.json`, `tests/auto_pilot/reports/cliq_export_chat_inactive_recheck_20260412_105646.json`)"
  - "live config `/tmp/zoho-test-config.json` now reports Cliq auth healthy (`oauthReady: true`), but cliq-155 DM-history probe still fails on `/conversations/{user}/messages` with `request_url_invalid`"
  - "live retrieval reruns can still hit Zoho refresh throttle (`Access Denied` too many requests) after rapid probe bursts; matrix collection should run in cooldown-safe batches"
  - "local multipart channel sends now probe plural `/messages` fallback variants after singular `/message` attempts (focused tests green), reducing endpoint-coverage risk while cliq-155 live matrix evidence is still being collected"
  - "nightly broad verification rerun is green (`make release-gate && make ci`: 321 tests + wheel smoke + fmt/lint, 2026-04-12T14:19:04Z), so no new unit/packaging regressions were found; remaining Cliq risk is live endpoint behavior (export scopes/retrieval parity/unsupported endpoint families)"
  - "cliq send scope-invalid regression is fixed in test coverage by mocking the direct `/channels/{id}/message` fallback path; `make release-gate` rerun is green (252 passed + wheel smoke), so remaining cliq-155 risk is live endpoint behavior, not unit regression"
  - "cliq-140 live search verification on happydistrouklimited returns unsupported endpoint patterns (`extra_param_found` on `/messages/search` with query key attempts and `request_url_invalid` on fallback paths); command now reports `not_supported` with capabilities hint"
  - "cliq-155 chat discovery live positive-path is now verified (`cliq chats` returns `count: 0` after re-auth), but native local multipart upload/download matrix evidence is still incomplete: probes still fail on tested targets with endpoint-level `request_url_invalid` / `operation_failed`; diagnostics now include `field=...` and compact `attempts:` path/field/status/code tuples for faster endpoint classification, and chat-scope failures now explicitly suggest `zoho cliq status --check-auth` before re-auth"
low: []
