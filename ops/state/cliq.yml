module: Cliq
updated_at: "2026-04-08T05:30:00Z"
status: in_progress
blockers:
  - live cliq send/notify still unverified; requires fresh token minted after latest scope bundle update
next_small_step: re-run `login --with-cliq` with latest code, then execute live `cliq send` and `cliq notify-mail` smoke checks
