id: cliq-channel-411
title: run fake and live verification gate for v0.4 native Cliq channel
module: openclaw_cliq_channel
stage: ready
status: ready
owner: coder
updated_at: '2026-05-05T08:44:45Z'
execution_mode: turbo_20x
throughput_policy_ref: ops/state/execution_policy.yml
success_criteria:
  - run package-local `npm run typecheck` and `npm run build`
  - run OpenClaw linked install, `plugins inspect zoho-cliq --json`, and `plugins doctor`
  - run focused OpenClaw channel tests plus lane docs tests
  - run fake host verification for webhook, polling, lifecycle, turn-ledger, native dispatch, redacted diagnostics, and outbound delivery
  - prepare live verification commands for the configured Zoho Cliq Bot/webhook without exposing secrets or raw message bodies
  - record live verification outcomes as pass/fail/skip_deferred with redacted diagnostic bundle evidence
  - keep `make ci` green before marking the v0.4 channel gate complete
next_if_done: prepare v0.4 native Cliq channel release candidate packaging and compatibility maintenance (`cliq-channel-412`).
progress:
  - "2026-05-05T08:44:45Z cliq-channel-415 completed: added `src/observability.ts` and `src/privacy.ts` for redacted audit events, stable correlation ids, diagnostic bundles, webhook/polling/lifecycle/turn-ledger/native dispatch rate-limit diagnostics, privacy retention rules, dead-letter replay guidance, and npm integrity checklist placeholders. Status diagnostics now advertise `observabilityBundle=true`, `productionReadiness=pending_live_verification`, blocker `live_verification_pending`, implemented slice `cliq-channel-415`, and `nextSlice=cliq-channel-411`."
  - "2026-05-05T08:25:45Z cliq-channel-417 completed: `src/native-dispatch.ts` maps accepted Cliq webhook/polling events into OpenClaw native channel turns, records session/last-route metadata, delivers agent replies through the Cliq outbound adapter, and fake host coverage verifies trusted webhook dispatch, trusted polling dispatch, denied events, duplicate suppression, and dispatch-failure dead-lettering."
next_step: Start `cliq-channel-411` by running fake/package-local verification, then prepare redacted live Bot webhook and polling verification commands for the configured environment.
