id: cliq-195
title: package cliq workflows for agent operation (internal loop + external-contact escalation path)
module: cliq
stage: in_progress
status: in_progress
owner: coder
updated_at: '2026-04-24T07:47:53Z'
success_criteria:
- add an initial cliq-195 operator-workflow packaging contract to watch-context output without breaking existing watch-loop consumers
- preserve the operator-workflow contract through watch-act action payload/result surfaces used by automation loops
- keep focused watch-context/watch-act verification green while deferred external blockers stay capability-gated
next_if_done: if focused and medium-scope cliq-195 verification remain green, continue the next operator-workflow packaging increment without regressing deferred capability-gated blockers.
next_step: "implemented one thin cliq-195 operator-workflow packaging increment so top-level watch payload kebab alias `default-action` now resolves in both `zoho cliq watch-act` and `zoho cliq bridge-run --watch-file` with preserved source provenance (`watchPayload.default-action`); focused checks are green (`./.venv/bin/python -m pytest -q tests/test_cli.py::test_cliq_watch_act_uses_top_level_watch_payload_default_action_kebab_alias tests/test_membrane_bridge.py::test_cliq_bridge_run_watch_file_infers_action_from_top_level_default_action_kebab_fallback` -> `2 passed in 1.13s`, `./.venv/bin/ruff format --check zoho_cli/cli.py tests/test_cli.py tests/test_membrane_bridge.py` -> `3 files already formatted`). next best step is a medium-scope cliq-195 integration refresh across watch-context/watch-act/bridge-run surfaces."
