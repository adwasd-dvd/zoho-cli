project: zoho-cli
updated_at: '2026-04-11T05:00:27Z'
status: cliq-expansion
canonical_workspace: /Users/adwasd/.openclaw/workspace-coder
project_root: /Users/adwasd/.openclaw/workspace-coder/job/zoho-cli
module_order:
  - Mail
  - Cliq
  - CRM
current_focus: cliq-155 dm/group/channel visibility and typed context matrix
primary_blockers:
  - crm live read-only verification remains blocked by account-level CRM org access
  - cliq search endpoint variants are not available on happydistrouklimited for current token/api combinations (`extra_param_found` / `request_url_invalid`)
  - "`cliq chats` now validates on live token/network after re-auth (`count: 0`), but DM/group attachment-positive evidence is still pending"
  - "current shared live config (`/tmp/zoho-test-config.json`) refreshes tokens again, but current grants are Mail-only, so cliq-155 matrix probes remain blocked until Cliq scopes are re-authorized"
  - native local-file upload/download behavior now has local multipart scaffolding (with image-field fallback preference, upload endpoint/field metadata, file/voice retrieval sourcePath metadata, raw+resolved user-target fallback, direct `/channels/{channel_id}/message` probing, plural `/messages` channel-send fallback variants, single-fetch channel descriptor resolution for channel-id sends, multipart failure diagnostics that include attempted form field metadata `field=...`, and compact `attempts:` path/field/status/code tuples); standard channel-id sends now also probe direct `/channels/{channel_id}/message`, but live endpoint matrix hardening is still pending because tested multipart sends continue to return `request_url_invalid` / `operation_failed` (evidence pending)
next_step: "re-run `zoho login --with-cliq` against `/tmp/zoho-test-config.json` to restore Cliq chat/message/write scopes, then rerun cliq-155 native local-file upload/download verification (`cliq send`/`cliq file`/`cliq voice`) and capture endpoint evidence (`path`, `field`, `mimeType`, `sourcePath`, plus failing endpoint+field+status/code tuples from `attempts:`) until one attachment-positive retrieval sample is recorded"
additional_info:
  - Token refresh failures now report explicit `invalid_client` guidance (check `client_id`/`client_secret` and re-run `zoho login`) instead of only emitting a generic missing `access_token` payload error.
  - OAuth login defaults are now aligned on `http://localhost:51821/callback` (including `--no-browser` fallback from legacy `example.com`), and callback success UI now renders scope-aware Mail/Cliq/CRM guidance.
