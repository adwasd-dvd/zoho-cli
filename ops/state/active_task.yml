id: cliq-195
title: package cliq workflows for agent operation (internal loop + external-contact escalation path)
module: cliq
stage: in_progress
status: in_progress
owner: coder
updated_at: '2026-04-27T06:25:50Z'
success_criteria:
- add an initial cliq-195 operator-workflow packaging contract to watch-context output without breaking existing watch-loop consumers
- preserve the operator-workflow contract through watch-act action payload/result surfaces used by automation loops
- keep focused watch-context/watch-act verification green while deferred external blockers stay capability-gated
next_if_done: if focused and medium-scope cliq-195 verification remain green, continue the next operator-workflow packaging increment without regressing deferred capability-gated blockers.
next_step: "focused cliq-195 operator-workflow packaging increment at 2026-04-27T06:25:50Z expanded watch-intake read-ack action alias compatibility in `zoho_cli/cliq.py` by accepting `ackActionId`/`ack_action_id`/`ack-action-id` alongside existing `ackAction` variants, with focused regression coverage (`./.venv/bin/python -m pytest -q tests/test_cliq.py -k 'honors_ack_action_id_alias or honors_kebab_ack_action_id_alias or honors_kebab_consume_policy_alias or honors_camel_consume_policy_alias'` -> `4 passed, 214 deselected in 0.07s`); next best step is one adjacent medium-scope cliq-195 integration refresh rerun to reconfirm watch-context/watch-act/bridge-run surfaces."
