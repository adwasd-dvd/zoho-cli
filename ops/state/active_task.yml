id: platform-203
title: expand membrane bridge presets and first product wrappers for hybrid rollout
module: platform
stage: in_progress
status: in_progress
owner: coder
updated_at: '2026-04-17T19:05:59Z'
success_criteria:
- ship membrane preset resolution (`zoho-cliq` / `zoho-crm`) with focused tests
- land first explicit product wrapper commands (`crm bridge-run --bridge membrane`, `cliq bridge-run --bridge membrane`, `cliq export-chats-bridge-run`, `cliq app-commands-bridge-run`)
- keep docs and state files aligned with hybrid rollout guidance
next_if_done: continue platform-201/202 modularization slices and map top-priority blocked cliq actions behind explicit bridge/capability gates
next_step: continue platform-203 by mapping the next highest-value cliq blocker to an explicit bridge-backed wrapper or capability-gated fallback path (next likely slice: app-command detail bridge wrapper after app-commands bridge-run).
