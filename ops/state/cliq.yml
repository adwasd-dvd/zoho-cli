module: Cliq
updated_at: "2026-04-08T05:16:53Z"
status: in_progress
blockers:
  - cliq send currently returns `invalid_oauthtscope` until test account re-auths with updated Cliq scope bundle
next_small_step: re-auth with latest `--with-cliq` scope bundle, then re-test live `cliq send` and `cliq notify-mail`
