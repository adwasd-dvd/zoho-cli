project: zoho-cli
updated_at: "2026-04-08T15:38:24Z"
status: crm-phase1
canonical_workspace: "/Users/adwasd/.openclaw/workspace-coder"
project_root: "/Users/adwasd/.openclaw/workspace-coder/job/zoho-cli"
module_order:
  - Mail
  - Cliq
  - CRM
current_focus: advance crm-002 read-only command implementation
primary_blockers:
  - crm live read-only smoke (`crm list --module Leads`) fails with `OAUTH_SCOPE_MISMATCH`; account needs CRM scopes via `zoho login --with-crm`
  - `make ci` still fails at `ruff format --check` (format drift remains)
next_step: complete `login --with-crm`, then rerun crm read-only smoke (`crm list/get/search`) and close crm-002
