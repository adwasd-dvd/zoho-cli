# Integrations

This directory contains optional integration material that is useful when embedding `zoho-cli` into local agent systems such as OpenClaw. Nothing here is required for the core CLI to work.
