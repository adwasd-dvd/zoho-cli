id: cliq-193
title: implement app-governance plane (apps/app-get/app-permissions/app-permission-get/app-installs/app-install-get/app-commands/app-command-get complete, continue next slice)
module: cliq
stage: in_progress
status: in_progress
owner: automation
updated_at: '2026-04-15T09:18:02Z'
success_criteria:
- ship the next cliq-193 app-governance command/client slice with focused tests
- run one focused live verification for that new slice and archive evidence
- keep cliq-165 export blocker evidence current while app-account activation remains external
next_if_done: take the next queued cliq-193 app-governance slice with focused coverage
next_step: "completed cliq-193 app-command list hardening so metadata-tagged rows that only expose action-style aliases (`action`/`Action`, plus `command`/`Command`) are treated as command-shaped and no longer dropped behind metadata wrappers; focused tests are green (`tests/test_cli.py::test_cliq_app_commands_prefers_action_hint_row_with_meta_over_metadata_only_data_list`, `tests/test_cli.py::test_cliq_app_commands_skips_metadata_only_data_list_and_uses_commands_list`, `tests/test_cli.py::test_cliq_app_commands_skips_empty_data_list_and_uses_commands_list`). paired focused live rerun (`cliq status --check-auth` + `cliq app-commands APP_PROBE_FAKE_20260415_091529`) confirms auth/export readiness still healthy while app-command list endpoints remain `not_supported`; next: take the next smallest app-command output-hardening increment while cliq-165 remains externally blocked on `inactive_appaccount_user`."
