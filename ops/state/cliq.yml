module: Cliq
updated_at: '2026-04-12T02:43:29Z'
status: completed
blockers:
- live `/messages/search` APIs on happydistrouklimited remain unsupported for current
  endpoint variants (`extra_param_found` / `request_url_invalid`), documented as endpoint
  limitation
- 'attachment retrieval parity remains endpoint-limited: post-upload media-visible
  messages still return zero payloads on `/attachments` (`cliq_single_token_retrieval_after_upload_20260411_192343.json`,
  `cliq_retrieval_recheck_after_success_20260411_192603.log`)'
- "maintenance export command surface (`cliq export-chats`) is implemented and response-shape normalization is hardened, but live Org.Admin-scope verification (`list` + `--chat-id`) still needs one archived probe sample"
next_small_step: run one live Org.Admin export verification (`cliq export-chats` list
  + `--chat-id`) and archive sample JSON; otherwise hold cliq train as completed for
  current endpoint limits
