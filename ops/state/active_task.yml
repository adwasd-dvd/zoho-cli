id: cliq-193
title: implement app-governance plane (apps/app-get/app-permissions complete, continue next slice)
module: cliq
stage: in_progress
status: in_progress
owner: automation
updated_at: '2026-04-13T03:27:49Z'
success_criteria:
- ship the next cliq-193 app-governance command/client slice with focused tests
- run one focused live verification for that new slice and archive evidence
- keep cliq-165 export blocker evidence current while app-account activation remains external
next_if_done: take the next queued cliq-193 app-governance slice with focused coverage
next_step: "cliq-193 third slice is now in code (`cliq app-permissions` + `list_app_permissions`) with focused tests passing; next step is one focused live verification (`cliq status --check-auth` + `cliq app-permissions APP_PROBE_FAKE_<timestamp>`) and evidence archival, then the next smallest app-governance command/client slice while cliq-165 remains externally blocked by inactive_appaccount_user"
