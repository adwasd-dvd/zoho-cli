module: Cliq
updated_at: '2026-04-09T10:38:34Z'
status: in_progress
blockers:
  - "live `/messages/search` APIs on happydistrouklimited still look unsupported for current endpoint variants (`extra_param_found` / `request_url_invalid`), so search remains limitation-documented"
  - "`/chats` listing is currently scope-blocked on the active token/network (`oauthtoken_scope_invalid`), so DM/group discovery needs a chat-read capable token for positive-path closure"
  - "native local-file upload now has multipart client/CLI baseline, but live endpoint matrix verification is still pending for outbound voice/image/file across token/network variants"
next_small_step: "Run one re-auth round with explicit chat-read scope to validate `cliq chats` positive path, then execute live native local-file upload probes (voice/image/file) to close remaining cliq-155 upload matrix evidence gaps"
