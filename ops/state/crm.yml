module: CRM
updated_at: "2026-05-12T13:40:58Z"
status: in_progress
phase: v0_5_readiness_action_boundary
blockers:
  - "operator-provided dedicated CRM fixture payload and cleanup approval required before Zoho-backed dry-run smoke and live fixture execution"
current_milestone: crm-026-readiness-action-boundary
last_completed:
  id: crm-026
  summary: "Added redacted `operatorReview.nextCommands` and `operatorReview.actionBoundary` to CRM fixture operator readiness bundles so blocked bundles expose only dry-run fixer ids and ready bundles expose live fixture approval as operator-only/Zoho-writing."
next_small_step: run `ops/scripts/crm_fixture_operator_packet.sh` against an operator-edited dedicated fixture payload and cleanup plan; execute only ids with `agentMayExecute=true`, and keep live fixture approval operator-only
