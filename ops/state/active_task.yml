id: platform-203
title: expand membrane bridge presets and first CRM wrapper for hybrid rollout
module: platform
stage: in_progress
status: in_progress
owner: coder
updated_at: '2026-04-17T17:56:30Z'
success_criteria:
- ship membrane preset resolution (`zoho-cliq` / `zoho-crm`) with focused tests
- land first explicit product wrapper command (`crm bridge-run --bridge membrane`)
- keep docs and state files aligned with hybrid rollout guidance
next_if_done: add first cliq wrapper command on the same bridge path and start mapping top-priority blocked cliq actions behind explicit fallback flags
next_step: implement one thin `cliq` wrapper command that can delegate through membrane via preset-based connection resolution.
