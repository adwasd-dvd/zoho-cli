id: cliq-channel-513
title: expose next operator action in RC autonomy JSON
module: openclaw_cliq_channel
stage: completed
status: completed
owner: coder
updated_at: '2026-05-13T21:02:00Z'
execution_mode: deep_milestone_batch
throughput_policy_ref: ops/state/execution_policy.yml
success_criteria:
  - "Expose the top pending operator request as `nextOperatorActionId` and `nextOperatorActionRequest`"
  - "Keep `save_openclaw_cliq_bot_message_handler` as the first action while direct Bot reliability is the leading blocker"
  - "Preserve non-executable publish/write boundaries and redaction flags"
  - "Update Lane 1/2/3 docs, tests, and RC checklist state"
progress:
  - "Added `nextOperatorActionId` and `nextOperatorActionRequest` to `ops/scripts/zoho_cli_rc_autonomy_packet.sh`."
  - "Added the same next-operator-action fields to `ops/scripts/zoho_cli_rc_operator_action_prompt.sh`."
  - "Updated operator-action tests and docs so heartbeat agents can read the real `oldsix老六` handler save/recheck ask without reparsing the full request list."
  - "Synced README, RC checklist, changelog, Lane 3 skill docs, OpenClaw guide, command playbook, and queue state."
next_if_done: Wait for the operator to paste/save the rendered Message Handler and send exactly one fresh direct `oldsix老六` Bot message, then run a single short-window no-response packet.
next_step: "Run `ops/scripts/zoho_cli_rc_operator_action_prompt.sh` and read `nextOperatorActionId`; it should be `save_openclaw_cliq_bot_message_handler`. After the operator saves the Message Handler and sends one fresh direct Bot message, run exactly one `ZOHO_CLIQ_INGRESS_LOOKBACK_SECONDS=600 ops/scripts/openclaw_cliq_bot_no_response_packet.sh` check."
