module: Cliq
updated_at: "2026-04-08T08:35:28Z"
status: in_progress
blockers:
  - live cliq send/notify still unverified; active token is missing `ZohoCliq.Webhooks.CREATE` and both commands now fail fast with `oauth_scope_invalid`
next_small_step: run `zoho login --with-cliq` to grant `ZohoCliq.Webhooks.CREATE`, then re-run live `cliq send` + `cliq notify-mail` on `happydistrouklimited`
