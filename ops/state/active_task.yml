id: cliq-193
title: implement app-governance plane (apps/app-get/app-permissions/app-permission-get/app-installs/app-install-get/app-commands/app-command-get complete, continue next slice)
module: cliq
stage: in_progress
status: in_progress
owner: automation
updated_at: '2026-04-14T15:03:04Z'
success_criteria:
- ship the next cliq-193 app-governance command/client slice with focused tests
- run one focused live verification for that new slice and archive evidence
- keep cliq-165 export blocker evidence current while app-account activation remains external
next_if_done: take the next queued cliq-193 app-governance slice with focused coverage
next_step: "implemented the next cliq-193 output-hardening increment: `zoho cliq app-commands` now prefers command-shaped rows over metadata rows when wrapper lists are metadata-first (for example `payload.data: [{meta: ...}, {command: {...}}]`). focused tests are green (`tests/test_cli.py::test_cliq_app_commands_prefers_command_rows_over_metadata_in_data_list`, `tests/test_cli.py::test_cliq_app_commands_accepts_top_level_payload_wrapper_shape`). next is one matching focused live rerun (`zoho cliq status --check-auth` + `zoho cliq app-commands APP_PROBE_FAKE_<stamp> --network happydistrouklimited`) while cliq-165 remains externally blocked by `inactive_appaccount_user`."
