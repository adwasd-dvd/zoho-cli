id: crm-008
title: implement CRM upsert dry-run command behind write-surface safety gates
module: crm
stage: in_progress
status: in_progress
owner: coder
updated_at: '2026-05-05T10:42:55Z'
execution_mode: turbo_20x
throughput_policy_ref: ops/state/execution_policy.yml
success_criteria:
  - add `zoho crm upsert` as dry-run by default with no live write path unless all gates are present
  - accept JSON payload input with field API names and compute a stable payload digest
  - require duplicate-check fields and idempotency key in the dry-run contract
  - keep live `--execute` blocked or exact-confirm gated until audit output is fully tested
  - update Lane 2/3 docs and Lane 1 state with the upsert dry-run command contract
next_if_done: consider live upsert execution only after dry-run contract and audit envelope are green.
progress:
  - "2026-05-05T10:42:55Z crm-007 completed: added `crm_write_surface_policy()` plus `zoho crm write-plan`; exposed `writeSurfacePolicy` through `zoho crm status`, `zoho crm sdk-status`, and `zoho crm write-plan`; locked `writesEnabled=false`, dry-run default, exact confirmation, idempotency-key, JSON-payload, and audit-envelope requirements; chose `upsert` as the first implementation candidate and kept `delete` blocked; added `docs/architecture/CRM_WRITE_SURFACE_CONTRACT.md`; refreshed Lane 2/3 docs and help snapshot; focused CRM/Lane 3 tests plus CLI diagnostic probes passed."
  - "2026-05-05T10:33:29Z crm-006 completed: locked `crm_api_version_policy()` in code and diagnostics; HTTP v2 remains the default CRM adapter/version to preserve current output shapes, SDK/API v8 remains explicit-only through `--adapter sdk-v8`, HTTP v8 base inference is available only for compatibility work, and `zoho crm status` plus `zoho crm sdk-status` expose `apiVersionPolicy`. Focused lint, format, CRM tests, Lane 3 docs tests, markdown update tests, and CLI diagnostic probes passed."
  - "2026-05-05T10:21:33Z crm-005 completed: added explicit `--adapter http-v2|sdk-v8` gates to `crm modules`, `crm fields`, `crm list`, `crm get`, and `crm search`; kept `http-v2` as default; initialized official SDK mode from the CLI-refreshed access token behind `zoho_cli/crm_sdk.py`; returned structured SDK availability errors; added fixture-backed parity coverage for SDK operation parameters and JSON-safe normalization; refreshed Lane 2/3 docs and help snapshot."
  - "2026-05-05T10:09:33Z crm-004 completed: added `zoho_cli/crm_sdk.py` as the optional official SDK boundary with data-center mapping for existing account hosts, CLI-managed resource/token cache paths with `ZOHO_CRM_SDK_RESOURCE_PATH` override, lazy SDK binding imports, `ZohoCrmSdkAdapter` read-only method skeletons, SDK response normalization into plain dict/list JSON, and `zoho crm sdk-status` `adapterSkeleton` diagnostics while preserving the current `http-v2` default."
  - "2026-05-05T09:49:08Z crm-003 completed: reviewed the existing CRM read-only surface and official Zoho CRM Python SDK v8 sources; added `zoho crm sdk-status`, optional `zoho-cli[crm-sdk]` packaging for `zohocrmsdk8_0==5.0.0`, `docs/architecture/CRM_V0_5_SDK_ADOPTION_PLAN.md`, Lane 2/3 docs, help snapshot, and focused CRM/CLI tests while keeping the current JSON-safe HTTP adapter as default."
  - "2026-05-05T09:42:24Z cliq-channel-418 completed: added `docs/releases/OPENCLAW_CLIQ_CHANNEL_V0_4_RC_CHECKLIST.md` with included scope, green evidence, remaining public Bot callback gate, RC cut steps, package integrity guidance, and non-goals. Native Cliq channel v0.4 is ready for local/operator RC testing; production rollout remains gated on public Bot callback reachability. Full `make ci` passed with `1777 passed in 49.10s`."
  - "2026-05-05T09:31:51Z cliq-channel-412 completed: added `docs/releases/OPENCLAW_CLIQ_CHANNEL_COMPATIBILITY.md`, refreshed the SDK contract and docs with OpenClaw compatibility matrix evidence, verified Temp-HOME linked install/inspect/doctor against global `2026.5.3-1`, npm latest `2026.5.4`, and npm beta `2026.5.4-beta.3` without raising the v0.4 host floor, and kept `make ci` green with `1776 passed in 48.78s`."
  - "2026-05-05T09:17:12Z cliq-channel-411 completed: added `ops/scripts/openclaw_cliq_live_smoke.sh` for redacted fake/live smoke checks, classified Zoho refresh throttling as `rate_limited`/`skip_deferred`, fixed webhook/inbound normalization so `message.id` is not misclassified as `chatId`, and recorded live evidence: OpenClaw plugin/channel checks pass; Zoho auth/capability/polling probes pass; local webhook auth/security gates pass; native polling returns an empty but valid event set; public Bot callback remains deferred until a reachable tunnel/gateway URL is configured. Full `make ci` passed with `1775 passed in 47.39s`."
  - "2026-05-05T08:44:45Z cliq-channel-415 completed: added `src/observability.ts` and `src/privacy.ts` for redacted audit events, stable correlation ids, diagnostic bundles, webhook/polling/lifecycle/turn-ledger/native dispatch rate-limit diagnostics, privacy retention rules, dead-letter replay guidance, and npm integrity checklist placeholders. Status diagnostics now advertise `observabilityBundle=true`, `productionReadiness=pending_live_verification`, blocker `live_verification_pending`, implemented slice `cliq-channel-415`, and `nextSlice=cliq-channel-411`."
  - "2026-05-05T08:25:45Z cliq-channel-417 completed: `src/native-dispatch.ts` maps accepted Cliq webhook/polling events into OpenClaw native channel turns, records session/last-route metadata, delivers agent replies through the Cliq outbound adapter, and fake host coverage verifies trusted webhook dispatch, trusted polling dispatch, denied events, duplicate suppression, and dispatch-failure dead-lettering."
next_step: Start `crm-008` by implementing `zoho crm upsert` dry-run output without enabling live CRM writes.
