id: cliq-channel-519
title: investigate Bot message reactions for Deluge-native direct messages
module: openclaw_cliq_channel
stage: pending
status: pending
owner: coder
updated_at: '2026-05-14T00:20:03Z'
execution_mode: deep_milestone_batch
throughput_policy_ref: ops/state/execution_policy.yml
success_criteria:
  - "Record the operator confirmation that the clean-response `oldsix老六` Message Handler now replies normally."
  - "Determine whether Zoho Bot Message Handler payloads expose a real inbound message id that can be used by `zoho cliq react` / `zoho cliq status-react`."
  - "If a real message id is available, pass it through the Deluge-native handler path and enable true lifecycle/status reactions with focused coverage."
  - "If no real message id is available, document the limitation and provide a tested fallback such as emoji-prefixed Bot replies without claiming true message reactions."
  - "Keep diagnostics privacy-safe: no raw message bodies, reply bodies, local paths, or secrets in reports."
progress:
  - "Operator confirmed the clean-response direct Bot path is now normal after Deluge returns only `response.text`."
  - "User requested emoji/reaction behavior for incoming Bot messages; queued this as `cliq-channel-519`."
  - "User also requested Discord-like Bot-to-agent selection and asked to bind `oldsix老六` to `zoho-employee-test`; cliq-channel-520 verified the current `cliq/default -> zoho-employee-test` binding and added deep-status diagnostics for it."
next_if_done: Return to RC publish-path selection (`cliq-channel-496`) and CRM fixture operator inputs unless another Bot reliability issue appears.
next_step: "Inspect current OpenClaw Cliq inbound/lifecycle code and Bot handler templates for message-id handling, then add a privacy-safe discovery/implementation slice for true reactions or a documented fallback."
