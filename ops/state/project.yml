project: zoho-cli
updated_at: '2026-04-12T01:05:47Z'
status: cliq-expansion
canonical_workspace: /Users/adwasd/.openclaw/workspace-coder
project_root: /Users/adwasd/.openclaw/workspace-coder/job/zoho-cli
module_order:
  - Mail
  - Cliq
  - CRM
current_focus: cliq-155 dm/group/channel visibility and typed context matrix
primary_blockers:
  - crm live read-only verification remains blocked by account-level CRM org access
  - cliq search endpoint variants are not available on happydistrouklimited for current token/api combinations (`extra_param_found` / `request_url_invalid`)
  - "`cliq chats` now validates on live token/network after re-auth (`count: 0`), but DM/group attachment-positive evidence is still pending"
  - "current live config auth is healthy (`oauthReady: true`, no missing Cliq scopes); DM history fallback endpoint coverage (`/conversations`, `/buddies`, `/users`, `/chats`) now degrades to empty history (`count: 0`) on full candidate miss instead of hard-failing, improving live probe stability on happydistrouklimited"
  - "native local-file upload/download behavior now has local multipart scaffolding (with image-field fallback preference, upload endpoint/field metadata, file/voice retrieval sourcePath metadata, raw+resolved user-target fallback, direct `/channels/{channel_id}/message` probing, plural `/messages` channel-send fallback variants, single-fetch channel descriptor resolution for channel-id sends, multipart failure diagnostics that include attempted form field metadata `field=...`, and compact `attempts:` path/field/status/code tuples); live probes now confirm text send success (`204`) while local image/file/voice uploads still fail with `request_url_invalid` / `operation_failed` / `request_method_invalid` (evidence captured)"
  - "latest retrieval rerun hit Zoho refresh throttle (`Access Denied` too many requests), so attachment-positive `cliq file`/`cliq voice` evidence still needs a cooldown rerun"
next_step: "execute the stack-top Cliq route in order: (1) rerun live probes to verify DM history fallback parity and capture winning endpoint path, (2) make image/voice/file local send reliable across user/channel targets, (3) capture attachment-positive retrieval evidence, and (4) classify any remaining failures as code defects vs Zoho endpoint limitations using `attempts:` tuples"
additional_info:
  - Auto-pilot probe scripts now resolve repo root from script location and can be launched from either project root or workspace root without path drift.
  - "2026-04-12 live check: `cliq status --check-auth` now reports `oauthReady: true`, and text send probes (user + channel) return `httpStatus: 204`."
  - Multipart local-upload fallback now treats `request_method_invalid` as path-level unsupported and skips that endpoint immediately, reducing wasted retries before probing the next fallback path.
  - Multipart local-upload fallback now also treats `operation_failed` as path-level unsupported; latest live local-image probe still fails, but exits without attempt-cap exhaustion and preserves compact `attempts:` evidence for endpoint classification.
  - Fresh user-target local-file probe evidence (`tests/auto_pilot/reports/cliq_local_user_file_20260411_180537.log`) still shows no attachment-positive path across buddy/user + email/zuid endpoint variants (`operation_failed` / `request_url_invalid`).
  - Token refresh failures now report explicit `invalid_client` guidance (check `client_id`/`client_secret` and re-run `zoho login`) instead of only emitting a generic missing `access_token` payload error.
  - OAuth login defaults are now aligned on `http://localhost:51821/callback` (including `--no-browser` fallback from legacy `example.com`), and callback success UI now renders scope-aware Mail/Cliq/CRM guidance.
  - "`zoho login` now normalizes repeatable `--scope` values via shared parsing, so comma-delimited scope strings no longer duplicate Cliq scopes in generated auth URLs when combined with `--with-cliq`."
