module: Cliq
updated_at: '2026-04-10T00:54:00Z'
status: in_progress
blockers:
  - "live `/messages/search` APIs on happydistrouklimited still look unsupported for current endpoint variants (`extra_param_found` / `request_url_invalid`), so search remains limitation-documented"
  - "`/chats` listing is currently scope-blocked on the active token/network (`oauthtoken_scope_invalid`), so DM/group discovery needs a chat-read capable token for positive-path closure"
  - "native local-file upload now has multipart client/CLI baseline (including image-field fallback preference, upload endpoint/field metadata, raw+resolved user-target fallback, direct `/channels/{channel_id}/message` fallback, single-fetch channel descriptor resolution for channel-id sends, and multipart failure diagnostics that include attempted form field metadata `field=...`); standard `cliq send --channel-id` also probes `/channels/{channel_id}/message`, and both `cliq file` + `cliq voice` report retrieval `sourcePath`, but live endpoint matrix verification is still pending because tested multipart sends currently fail with endpoint-level `request_url_invalid` / `operation_failed` responses"
next_small_step: "Re-run `zoho login --with-cliq` (now includes `ZohoCliq.Chats.ALL`) and validate `cliq chats` positive path, then execute live native local-file upload probes (voice/image/file) plus `cliq file`/`cliq voice` retrieval probes and record returned upload/download metadata (`path`, `field`, `mimeType`, `sourcePath`) along with failing endpoint+field tuples from `api_error`, explicitly checking whether direct `/channels/{channel_id}/message` fallback improves both standard and multipart channel-id send reliability"
