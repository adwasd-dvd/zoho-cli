# Current Focus

## Immediate
1. **`cliq-172` parity closeout**: run targeted live search + attachment retrieval probes, refresh endpoint taxonomy evidence, and keep blocker-skip logs current.
2. **Keep blocker-skipping loop active**: do not stall on `cliq-165` export-scope re-auth or endpoint limitations; continue with queued Cliq work when human interaction is required.
3. **Scheduled/thread status**: both planes are now classified as endpoint limitations on happydistrouklimited (`tests/auto_pilot/reports/cliq_scheduled_probe_summary_20260412_132412.json`, `tests/auto_pilot/reports/cliq_thread_probe_summary_20260412_044149.json`).

## Next
1. `cliq-172`: search + attachment retrieval parity closeout
2. `cliq-180` / `cliq-181`: bot operations + chat control plane expansion
3. `cliq-165`: rerun export verification after interactive re-auth with maintenance export scopes
